<?xml version="1.0" encoding="utf-8"?>
<!-- Widget ichki kartasi (streak / goal / stats / week) -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/it_widget_tile" />
    <corners android:radius="16dp" />
    <stroke
        android:width="1dp"
        android:color="@color/it_widget_tile_stroke" />
</shape>
