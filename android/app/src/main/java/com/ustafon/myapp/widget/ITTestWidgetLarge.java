package com.ustafon.myapp.widget;

/** ITTest LARGE widget (4x3) — asosiy reference widget:
 *  logo + tagline, goal ring, blue flame streak, robot,
 *  3 stat tile, weekly row (Du..Ya). Barchasi REAL data. */
public class ITTestWidgetLarge extends ITTestWidgetProvider {
    @Override
    protected int layoutId() {
        return com.ustafon.myapp.R.layout.ittest_widget_large;
    }
}
